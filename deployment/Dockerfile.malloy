FROM node:20-slim

WORKDIR /app

RUN npm install -g @malloy-publisher/server

COPY analytics/ ./analytics/

EXPOSE 8080

CMD ["npx", "@malloy-publisher/server", "--config", "analytics/malloy-config.json", "--port", "8080", "--host", "0.0.0.0"]
