# AGENTS.md /orchestration

This directory contains Dagster orchestration for Orca data warehouse. 

To list Dagster assets use `dg list defs`.
To execute the data warehouse use `dg launch --assets "*"`.